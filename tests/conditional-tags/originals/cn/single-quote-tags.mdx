# 单引号标签测试用例 - cn

## 1. 单引号区域标签 - region='global'

<Conditional region='global'>

这是全局区域内容，在cn目录下应该被删除，包括其包含的表格等内容。

| 标题1 | 标题2 | 标题3 |
|---|---|---|
| 内容1 | 内容2 | 内容3 |
| 数据1 | 数据2 | 数据3 |

</Conditional>

## 2. 单引号区域标签 - region='cn'

<Conditional region='cn'>

这是CN区域内容，在cn目录下应该保留，但删除标签。

</Conditional>

## 3. 单引号与双引号混合使用

<Conditional region='global' brand="tds">

这是混合使用单引号与双引号的内容，在cn目录下应该整个被删除。

</Conditional>

<Conditional region="cn" brand='tds'>

这是混合使用双引号与单引号的内容，在cn目录下应该保留内容，但删除标签。

</Conditional>

## 预期处理结果

处理后，应该：
1. 删除全局区域(region='global')的标签及其内容，包括表格
2. 保留CN区域(region='cn')的内容，但删除标签
3. 删除region='global'的混合标签及其内容
4. 保留region="cn"的混合标签内容，但删除标签 